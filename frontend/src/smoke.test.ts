import { describe, it, expect } from 'vitest';

describe('frontend smoke test', () => {
  it('arithmetic still works (placeholder until WP2 adds real tests)', () => {
    expect(1 + 1).toBe(2);
  });
});
