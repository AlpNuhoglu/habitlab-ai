{
  "name": "@habitlab/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types between backend and frontend.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "echo 'no tests in shared yet' && exit 0",
    "lint": "echo 'no lint in shared yet' && exit 0",
    "typecheck": "tsc --noEmit",
    "build": "echo 'shared is consumed as source; nothing to build' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
