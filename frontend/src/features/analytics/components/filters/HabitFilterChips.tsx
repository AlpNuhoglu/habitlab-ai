// Stub — multi-select habit filter deferred to post-WP5.
// Accepts no props and renders nothing; typed stub keeps the component slot reserved.
export function HabitFilterChips(): null {
  return null;
}
